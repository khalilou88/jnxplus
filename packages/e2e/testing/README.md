# e2e-testing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build e2e-testing` to build the library.

## Running unit tests

Run `nx test e2e-testing` to execute the unit tests via [Jest](https://jestjs.io).
