<% if(configFormat === '.properties' && isCustomPort) { -%>
micronaut.application.name=<%= projectName %>
micronaut.server.port=<%= port %>
netty.default.allocator.max-order=3
<% } -%>
<% if(configFormat === '.yml' && isCustomPort) { -%>
micronaut:
  application:
    name: micronautguide
  server:
    port: <%= port %>
netty:
  default:
    allocator:
      max-order: 3
<% } -%>
  