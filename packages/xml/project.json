{
  "name": "xml",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/xml/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/xml",
        "main": "packages/xml/src/index.ts",
        "packageJson": "packages/xml/package.json",
        "tsConfig": "packages/xml/tsconfig.lib.json",
        "assets": [
          "packages/xml/*.md",
          {
            "input": "./packages/xml/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./packages/xml/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          }
        ]
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs xml {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/xml/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/xml/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "angular",
        "trackDeps": true,
        "push": true,
        "noVerify": true,
        "postTargets": ["xml:update-xml-deps", "xml:github", "xml:deploy"],
        "commitMessageFormat": "release: cut release ${version} of {projectName}"
      }
    },
    "update-xml-deps": {
      "executor": "@jnxplus/internal-gradle-executors:update-xml-deps",
      "options": {
        "version": "${version}"
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "${tag}",
        "notes": "${notes}"
      }
    },
    "deploy": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public"
      }
    }
  },
  "tags": []
}
